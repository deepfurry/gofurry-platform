server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    # Same-origin API routing belongs to the deployment reverse proxy.
    location /api/ { return 503; }
}
